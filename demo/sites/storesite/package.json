{
  "name": "@solidlab/uma-demo-store",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "n3": "^1.17.3"
  },
  "scripts": {
    "start": "yarn run -T serve -s build -l 5002",
    "build": "yarn run -T react-scripts build"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
